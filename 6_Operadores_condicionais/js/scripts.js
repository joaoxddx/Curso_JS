if (true/*pode ser colocado uma variavel ou operadores logicos para validar isso*/) {
    console.log("Testando o if")
}

const number = 10;

let testeNumber = number < 5 ? 'Si' : 'No'; // IF ternario muito usado para regras simples

console.log(testeNumber);
